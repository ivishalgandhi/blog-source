# Now: /speckit.specify

Switch to Windsurf Cascade and run:

```
/speckit.specify
Build a PostgreSQL health check scanner with FastAPI and Typer CLI.
Checks: connectivity, latency, connection pool.
Single scan command reports overall status.
```

---

Watch Cascade generate `spec.md` with:
- Numbered user stories (US-1, US-2 …)
- Acceptance criteria in GIVEN / WHEN / THEN
- Edge cases and constraints

**Review it before moving on. This is the contract.**
