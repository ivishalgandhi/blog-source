# Now: /speckit.tasks

Switch to Windsurf Cascade and run:

```
/speckit.tasks
```

---

Watch Cascade generate `tasks.md` with:
- Ordered task list (T01, T02 …)
- Explicit dependencies between tasks
- `[P]` markers for tasks safe to run in parallel

**Notice: T03 "write tests" always before T04 "implement".**
That is Article II (Test-First) being enforced automatically.
