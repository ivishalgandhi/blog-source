# LAB-03-A: 3-Node Patroni + etcd Cluster
# This lab is fully self-contained. No secrets or credentials are required.
# Docker Compose handles all configuration internally.
#
# Optional: set a custom project name for Docker Compose
# COMPOSE_PROJECT_NAME=lab-03-a
